name: Amazon Test

on:
  push:
    branches: feature/Day-3

env:
  PRODUCT_ID: ABC123

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: npm test
